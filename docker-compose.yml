services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - waste-data:/app/data
    environment:
      - AWS_API_URL=${AWS_API_URL:-}
    restart: unless-stopped
volumes:
  waste-data:
